# state.md -- casework-tabs (session cache, overwritten each wind-down)

## Status

P1 CALENDAR PASSED 2026-08-10 (s3), receipt at
verify/gate-receipts/p1-calendar.md. Four gate-fed fix rounds
(spacing r1-r3, typography r4) landed during the live drive; rail
7 pass / 23 pending / 0 fail, sha d1a45962; all four standing
suites green and quoted in the receipt. Pushed to origin
(github.com/JamesIsHere/workshop-atlas) at James's request.

## Next actions

1. P2 TASKS per plan.md: extend/refine the four tasks rail steps,
   drive each RED, then build -- /tasks/quick (type-and-Enter),
   my-open default + firm/completed toggles, one-click complete,
   /settings/task-lists builder + matter import. Machinery home:
   task lists build HERE, re-home into Settings layout at P6.
2. [Q1]-[Q8] carry to the P2 gate (James's wrap-up call at P1;
   receipt records the disposition). [Q1] alone gates a rail step
   (calendar derived kinds stays PENDING until ruled).

## Watch items and caveats

- Demo server may be UP on port 8500 over data/demo-tabs.db
  (background task from this session). Fresh regen + restart at
  the P2 gate; same port always.
- Gate receipts quote the FULL step table (METHOD s3): marker
  regressions demote to PENDING, so the verdict line alone can
  hide a regressed screen.
- Calendar typography rulings (0.85rem tables, links always blue)
  are SCOPED to calendar surfaces; extending app-wide touches
  billing-ui's signed tables -- needs an explicit ruling.
- Typed deferral owed: final-walk TIME BUDGET at P7 walk-sheet
  ratification.
- casework-ui remains ON HOLD (cold runner); its data/ carries
  real-ish PII -- never seed or demo from it.
- Owed per phase: deliberate RED per new step + the empty-state
  FAIL arm at the phase that first renders a designed empty state
  with actions.
- Parked with triggers: "+N more" month-cell overflow (real
  density); matter/client in month cell or hover (James asks, or
  cross-tab walk shows scan confusion); interleaved notes
  timeline (rendered-artifact gate); calendar sync (meeting
  question); role ENFORCEMENT (future contract).

## Open decisions

- [Q1]-[Q8] queued for the P2 gate (worklog s3 list). None block
  the P2 build.
