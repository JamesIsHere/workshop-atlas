# state.md -- casework-tabs (session cache, overwritten each wind-down)

## Status

P4a FILES MECHANICS PASSED 2026-08-10 (s6, "yes pass", ZERO fix
rounds -- receipt verify/gate-receipts/p4a-files.md; ingestion
question answered closed from corpus + schema). P4b E-SIGN BUILT
same session, GATE PENDING: prepare POST from the PDF detail,
prep editor (signers/fields/request), live client_base signer
links, sign-through proven on the frozen client surface, stamped
copy in produced custody. 5 RED drives total this session, all
caught. Rail 22 pass / 9 pending / 0 fail, sha c44c5e31 x2
(supersedes 81168b79); all four standing suites green, quoted in
worklog s6. Demo reseeded, server UP on 8500.

## Next actions

1. P4b GATE: James drives e-sign on 8500 -- suggested drive:
   open SYNTH-g28-anya.pdf (draft: Continue preparing), or
   SYNTH-retainer-boris.pdf (requested: live link visible), or
   prepare a fresh PDF end-to-end; open the live link (client
   surface, port 8501), sign typed, watch the signed copy get
   auto-filed as produced. Verdict + receipt closes P4.
2. After the P4b verdict: P5 SEARCH per plan.md (chrome bar as
   the gate-ruled additive shared-chrome change, widened
   coverage readers, recents, grouped results; three rail steps
   exist, refine-first per rhythm).
3. [Q1]-[Q11] + [Q13] carry to the P4b gate.

## Watch items and caveats

- Demo server UP on 8500 over data/demo-tabs.db (background task
  byuta8xs4 this session); client surface on 8501 (its bare /
  404s by design -- it serves only token links). Demo servers
  SURVIVE sessions and hold the db lock -- check the port and
  kill before reseeding (s5's PID 36600, s6's 4328 both needed
  this).
- E-sign ground truth (discovered s6, do not re-derive): the
  frozen core mails the RELATIVE /esign/<token>; the absolute
  live link is the staff page's job (client_base, intake
  precedent). Client sign inputs are field_<id>; signature
  values JSON, typed mode {"mode":"type","text":...}. Completion
  auto-files <stem>-signed.pdf as produced under the source
  file's contact/matter.
- Typography (0.85rem td, firm-blue links) + tab-detail kv +
  matter-first rulings cover calendar, tasks, notes AND files;
  search/settings adopt at P5/P6; billing-ui's signed tables
  stay untouched.
- Gate receipts quote the FULL step table (METHOD s3).
- Sabotages via Edit-with-known-content ONLY (METHOD s4).
- Typed deferral owed: final-walk TIME BUDGET at P7 walk-sheet
  ratification.
- casework-ui remains ON HOLD (cold runner); its data/ carries
  real-ish PII -- never seed or demo from it.
- Owed per phase: deliberate RED per new step. Empty-state FAIL
  arm owed only for a NEW designed-empty pattern.
- Parked with triggers (unchanged): note attachments; single-note
  PDF (core-amendment family); "+N more" month-cell overflow;
  matter/client in month cell or hover; interleaved notes
  timeline; calendar sync; role ENFORCEMENT.

## Open decisions

- [Q1]-[Q11] + [Q13] queued for the P4b gate. None block P5.
