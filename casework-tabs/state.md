# state.md -- casework-tabs (session cache, overwritten each wind-down)

## Status

RATIFIED 2026-08-10, pre-P0. goal.md signed by James after one
red-pen round + the deferral-load check; Appendix B ratified into
atlas/CLAUDE.md the same signature (app_ui write surface is now
authorized). Roster row added; tab-review-notes.md retired.

## Next actions

1. U0.1 (P0 harness): casework-tabs/verify/ scaffold --
   run_tabs_walk.py skeleton + seed script (casework/app module
   calls only) + float/ISO/empty-state sweeps; drive the rail RED
   before any app_ui change (oracle-first + verify-the-verifier).
2. Then P1 Calendar per plan.md's per-phase rhythm.

## Watch items and caveats

- Typed deferral owed: final-walk TIME BUDGET is ruled at P7
  walk-sheet ratification (billing-ui's 25:00 is precedent).
- billing-ui's demo server may still be UP on port 8500 over the
  walked -09b db (James's asset). This child's gate dbs are fresh
  seeded dbs; coordinate the port at gate time.
- casework-ui remains ON HOLD (cold runner); its data/ carries
  real-ish PII -- never seed or demo from it.
- Parked with triggers: interleaved notes timeline (rendered-
  artifact gate, named idea); calendar sync (meeting question);
  role ENFORCEMENT (future contract).

## Open decisions

- None queued. [Q] ruling queue is empty; first entries accumulate
  during P0/P1 and clear at the P1 gate.
